(in-package :cl-user)
(defpackage caveleton-test
  (:use :cl
        :caveleton
        :prove))
(in-package :caveleton-test)

;; NOTE: To run this test file, execute `(asdf:test-system :caveleton)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
